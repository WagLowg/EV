# Example environment variables for frontend (Vite)
# On Vercel set this as an Environment Variable (Name: VITE_API_BASE_URL)
# Use the backend Render URL (no trailing slash). Example:
VITE_API_BASE_URL=https://ev-service-center-maintance-management-um2j.onrender.com

# For local development you can leave this empty and the frontend will use http://localhost:10000
# If your backend exposes API under /api already, the frontend will append /api automatically.
